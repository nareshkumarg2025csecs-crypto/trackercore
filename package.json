{
  "name": "TrackerCore-monorepo",
  "version": "1.0.0",
  "description": "TrackerCore Monorepo containing frontend and backend workspaces",
  "private": true,
  "scripts": {
    "start": "npm run build:frontend && cd backend && npm start",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "emulator": "firebase emulators:start",
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:full": "concurrently \"firebase emulators:start\" \"cd frontend && npm run dev\"",
    "build:frontend": "cd frontend && npm run build",
    "install:all": "cd frontend && npm install && cd ../backend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
